# Iceberg REST Experiment

This older experiment note has been folded into
`CATALOG_CAPABILITY_DEMO.md`.

The current branch now uses one `catalogs.yml`, one `catalog_demo` DuckDB
target, and the regular jaffle-shop DAG with model-level `catalog` config.
