sources:
  - name: temp
    database: development
    schema: fusion_tests_schema
    config:
      freshness:
        warn_after:
          count: 24
          period: hour
    tables:
      - name: m1
